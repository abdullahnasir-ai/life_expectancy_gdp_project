import matplotlib.pyplot as plt
from src.data_loader import df
import src.data_loader

plt.figure(figsize = (10, 8))
for i in range(1, len(df.unique(df['Country'])) + 1):
    country =
    plt.subplot(2, 3, i)
    timegdp

